import type { NextPage } from "next";

import { Layout } from "../shared/SharedComponents/Layout";

const Home: NextPage = () => {
	return (
		<Layout>
			<h1>hi</h1>
		</Layout>
	);
};

export default Home;
